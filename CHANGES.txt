

2017-12-16

* Implemented GUI using Tk backend. Activated when `mstm_studio.py` or `mstm_studio_support.py` script is run.

* Implemented analytical AuAg alloy dielectric function (refraction indeces).


2017-11-04 

* Fit procedure is split in two steps now. In internal (fast) step the scale and background parameters are varied, so that no recalculation of the spectra required.

2019-04-15

* Fixed undefined behaviour originated from ordering of keys in dictionary containing varied parameters 

* Extended capabilities for fitting of non-MSTM contributions
